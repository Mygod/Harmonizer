<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                xmlns:android="http://schemas.android.com/apk/res/android">
    <TextView android:id="@+id/welcomingText"
              android:layout_width="fill_parent"
              android:layout_height="wrap_content"
              android:text="@string/welcoming_message" />
    <EditText android:id="@+id/frequencyText"
              android:layout_width="fill_parent"
              android:layout_height="wrap_content"
              android:layout_marginTop="16.0dip"
              android:hint="@string/frequency_hint"
              android:ems="10"
              android:layout_below="@id/welcomingText"
              android:inputType="numberDecimal"
              android:layout_alignEnd="@id/welcomingText"
              android:layout_alignParentStart="true" />
    <Button android:id="@+id/beep_button"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginTop="16.0dip"
            android:text="@string/beep"
            android:layout_below="@id/frequencyText" />
</RelativeLayout>